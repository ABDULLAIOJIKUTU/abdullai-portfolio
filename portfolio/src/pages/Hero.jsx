import React from "react";
import Button from "../../public/components/Button";

const Hero = () => {
  return (
    <div>
      <Button name={"Download CV"} />
    </div>
  );
};

export default Hero;
